结合多个优秀的开源项目与 Laravel 社区公认的最佳实践，我为你生成了适用于 **Laravel + PHP 8.1+** 项目的全套配置。所有规则文件存放在 `.lingma/rules/` 目录，Skill 文件存放在 `.agents/skills/` 目录（与通义灵码完美兼容）。

### ⚙️ 通义灵码 Project Rules 配置框架（Laravel 版本）

结合 `laraveldaily.com` 作者的实际 AI 规则、Spatie 官方发布的 AI 优化编码指南以及 `php-expert` Skill 的严格规范，整理出四份核心规则文件：

---

#### 规则一：全局 PHP / Laravel 编码规范 (`global.md`)

Spatie 开源的 `guidelines-skills` 包包含了 PSR-12 标准、类型声明、早期返回等核心规范。`php-expert` Skill 则强调 `declare(strict_types=1)` 是 PHP 文件的第一道防线，并禁止使用 `md5()`/`sha1()` 处理密码。以下规则融合了这些开源项目的最佳实践：

```markdown
# Laravel / PHP 全局编码规范

## **[P0] 铁律级别——绝对不可违反**
- 每个 PHP 文件第一行必须是 `declare(strict_types=1);`。
- 严禁使用 `md5()` 或 `sha1()` 处理密码，统一使用 `Hash::make()`。
- 严禁在控制器或 Blade 模板中编写原始 SQL 查询，所有查询必须走 Eloquent ORM 或 Query Builder。
- 所有用户输入必须在控制器边界进行验证（Form Request 或 `$request->validate()`）。
- 绝对不要 `catch (\Exception $e)` 吞掉异常而不做处理或重新抛出。

## **[P1] 命名与代码风格**
- 遵循 PSR-12 编码标准。文件使用 kebab-case，类使用 PascalCase，方法使用 camelCase，变量使用 snake_case。
- 优先使用 `match` 操作符而非 `switch`。
- 优先使用 Laravel 全局辅助函数而非 Facade 静态调用。例如：使用 `auth()->id()` 而非 `Auth::id()`，使用 `redirect()->route()` 而非 `Redirect::route()`，使用 `str()->slug()` 而非 `Str::slug()`。
- 避免声明只使用一次的临时变量（如 `$currentUser = auth()->user()`），直接链式调用即可。

## **[P2] 注释规范**
- 不要在显而易见的代码或变量上方生成注释。
- 不要在变量定义时添加 `/** @var */` docblock 注释，除非明确要求。
- 仅在逻辑复杂、需要额外解释"为什么这么写"的地方添加注释。
```

#### 规则二：控制器与服务层 (`controllers-services.md`)

`laravel-architecture` Skill 明确要求"瘦控制器"——将业务逻辑委托给 Service 类。Laravel Daily 的建议则更加具体：若 Service 仅被一个控制器方法使用，直接在该方法中注入；若被多个方法使用，则在构造函数中注入。以下是融合版本：

```markdown
# 控制器与服务层架构规范

## **[P0] 瘦控制器原则**
- 控制器方法不超过 7 行代码。复杂的业务逻辑必须提取到 `app/Services/` 目录。
- 控制器只做三件事：接收请求 → 调用 Service → 返回响应。

## **[P1] Service 注入策略**
- 若 Service 类仅被该控制器的一个方法使用，直接在该方法中通过参数注入（方法级注入）。
- 若 Service 类被控制器中多个方法使用，在构造函数中注入。
- 所有依赖注入统一通过接口（`app/Contracts/`）实现，以支持测试 Mock。

## **[P2] Form Request 与枚举**
- 所有表单验证必须使用独立的 Form Request 类（`php artisan make:request`），不要在控制器中写验证逻辑。
- 所有枚举类统一存放在 `app/Enums/` 目录。
- 若数据库列的值来自枚举，必须在迁移文件中将枚举值设为默认值，并在模型中强制转换为枚举类型。
- 在 Blade 模板和测试代码中，凡是涉及枚举字段的地方，一律使用枚举类引用而非硬编码字符串。

## **[P3] Observer 注册方式**
- Eloquent Observer 统一使用 PHP 8 的 Attribute 方式注册在模型中，不要在 `AppServiceProvider::boot()` 中注册。
- 示例：`#[ObservedBy([UserObserver::class])]`
```

#### 规则三：Eloquent ORM 与数据库 (`eloquent-database.md`)

`php-expert` Skill 强调始终使用 Eloquent 而非原始 SQL，并推荐 `Repository` 模式作为数据访问层。`laravel` Skill 则特别强调使用 `eager loading` 预防 N+1 查询。融合后的规则如下：

```markdown
# Eloquent ORM 与数据库规范

## **[P0] 安全与查询**
- 严禁在任何地方拼接原始 SQL 字符串。使用 Eloquent ORM 或 Query Builder 的参数化查询。
- 严禁在 `.env` 或配置文件中提交数据库密码等敏感信息。生产密码必须通过环境变量注入。

## **[P1] 模型与关系**
- Eloquent 模型只承担三种职责：属性转换（casts）、关系定义（relationships）和简单访问器（accessors）。
- 不要在模型中写业务逻辑或复杂的查询作用域，这些应提取到 Repository 或 Service 中。
- 所有关联关系必须在模型中显式声明，并添加 PHPDoc 类型提示。

## **[P2] 性能优化**
- 使用 `with()` 进行预加载，预防 N+1 查询问题。
- 在需要分页的列表查询中，统一使用 `->paginate()` 而非 `->get()`。
- 若业务允许，优先使用 `chunk()` 处理大批量数据，避免内存溢出。
- 在 `where()` 条件中，不要使用 `whereKey()` 或 `whereKeyNot()`，改用 `->where('id', ...)`。
- 为高频查询的数据库列添加合适的索引。

## **[P3] 迁移与枚举**
- 若某个列的值来源于枚举，在迁移文件中以枚举值作为 `default`。
- 在对应模型中添加 `$casts` 将该列强制转换为枚举类型。
- 迁移文件命名清晰，一个迁移只做一件事。
```

#### 规则四：测试规范 (`testing.md`)

Laravel 社区普遍推荐使用 Pest 框架进行测试。Laravel Daily 的作者也在其规则中明确要求"新功能必须生成 Pest 测试"。`multicoin/laravel-code-standards` 则在 CI/CD 流程中集成了 PHPStan Level 8 + Pint + PHPCS 全自动检查。结合这些实践：

```markdown
# Laravel 测试规范

## **[P0] 测试要求**
- 新功能必须生成 Pest 自动化测试（`.tests/Feature/` 或 `.tests/Unit/`）。
- 核心业务逻辑的代码覆盖率目标 > 80%。
- 测试必须覆盖：正向场景（Happy Path）、反向场景（异常/非法输入）、边界场景。

## **[P1] 测试工具与写法**
- 使用 Pest 框架进行单元测试和功能测试，使用 Laravel Dusk 进行浏览器端到端测试。
- 所有测试都应在每个独立场景开始时使用 `RefreshDatabase` trait 或 `DatabaseTransactions`。
- 使用模型工厂（Model Factories）创建测试数据，不要手动插入。
- 对外部服务（第三方 API、邮件、队列）使用 Mock 或 Fake。

## **[P2] 命名与组织**
- 测试文件命名为 `*Test.php`，放在与被测代码对应的目录结构中。
- 测试方法名使用 `it_` 前缀（Pest 风格），清晰描述测试场景。
- CI/CD 管线中必须包含以下脚本：
  - `composer lint`：执行 Laravel Pint 代码风格检查
  - `composer analyse`：执行 PHPStan 静态分析（Level 8 目标）
  - `composer test`：执行 Pest 测试套件
```

---

### 🧩 通义灵码 Agent Skills 配置框架（Laravel 版本）

Skill 规范参考了社区中多个成熟的 SKILL.md 定义，以及 `anilcancakir/laravel-ai-sdk-skills` 的格式标准。

---

#### Skill 一：Laravel 代码审查员 (`laravel-reviewer`)

Spatie 的 `guidelines-skills` 包含了专门的安全审查 Skill，覆盖 SSL、CSRF 保护、密码哈希和数据库权限等。本 Skill 融合了这些安全检查点。

在 `.agents/skills/laravel-reviewer/` 目录下创建 `SKILL.md`：

```markdown
---
name: laravel-reviewer
description: >-
  对 Laravel 项目的提交或指定代码进行深度审查。
  当用户提到"审查"、"review"、"检查代码"、"code review"时使用。
  重点检查 SQL 注入、N+1 查询、路由安全、权限校验和性能问题。
allowed-tools: "Read,Grep,WebFetch,Bash"
---

# Laravel 全栈代码审查员

你是一位精通 Laravel 和 PHP 8.x 的全栈工程师，擅长发现代码中的安全漏洞、性能瓶颈和架构问题。

## 审查流程
1. **理解变更意图**：首先运行 `git diff` 或仔细阅读用户提供的代码片段，理解本次修改的业务目的。
2. **分层审查**（按严重程度）：
   - **[P0] 安全审查**：SQL 注入（是否有原始拼接？）、XSS 漏洞（是否正确使用 `{{ }}`？）、CSRF 保护、密码哈希方式、敏感信息泄露（`.env` 是否被误提交？）。
   - **[P1] 架构审查**：控制器是否过"胖"？业务逻辑是否在 Service 层？命名是否符合 PSR-12？是否有重复代码？
   - **[P2] 性能审查**：是否存在 N+1 查询？是否缺少 eager loading？是否缺少必要的缓存？队列任务是否合理使用？
   - **[P3] 测试审查**：是否有对应的测试？测试是否覆盖了核心业务路径？
3. **生成审查报告**：按文件路径和严重程度，生成结构化的 Markdown 报告。

## 输出格式
### 📋 代码审查报告
- **总体评价**: (一句话总结本次变更的质量)
- **🔴 严重问题 (P0)**: (必须修复，涉及安全或数据完整性)
  - `文件路径:行号` - 问题描述与修复建议
- **🟡 改进建议 (P1)**: (应该修复，涉及架构或可维护性)
  - `文件路径:行号` - 优化方案
- **🟢 风格建议 (P2)**: (可选修复)
  - `文件路径:行号` - 代码片段示例
```

#### Skill 二：Pest 测试生成器 (`pest-test-generator`)

结合 Laravel 社区的 Pest 测试推荐和 `laravel` Skill 关于测试的规范。

在 `.agents/skills/pest-test-generator/` 目录下创建 `SKILL.md`：

```markdown
---
name: pest-test-generator
description: >-
  为指定的 Laravel 代码文件自动生成 Pest 测试。
  当用户提到"生成测试"、"写测试"、"add test"时触发。
  默认使用 Pest 框架、RefreshDatabase trait 和 Model Factories。
---

# Pest 测试自动化生成器

你是一位资深的 Laravel 测试工程师，精通 Pest 测试框架和 Laravel 的测试生态。

## 工作流程
1. **分析源代码**：仔细阅读用户指定的 Controller、Service、Model 或 Form Request 文件，识别所有需要测试的单元。
2. **设计测试用例**：为每个功能点设计覆盖以下场景的测试：
   - **正向测试**：正常输入下功能的正确性。
   - **反向测试**：非法输入、未授权访问、缺失参数的验证。
   - **边界测试**：空值、null、undefined、极限值的情况。
3. **生成测试文件**：
   - 遵循 Laravel 的测试目录结构，在 `tests/Feature/` 或 `tests/Unit/` 下创建对应的测试文件。
   - 使用 Model Factories 创建测试数据，不手动插入数据库记录。
   - 对外部依赖（API 调用、邮件、队列、事件）使用 `Http::fake()`、`Mail::fake()`、`Bus::fake()`、`Event::fake()` 进行 Mock。
   - 使用 `actingAs()` 方法模拟用户登录状态进行授权测试。
   - 所有增删改操作都应断言数据库最终状态（`assertDatabaseHas` / `assertDatabaseMissing`）。

## 关键规范
- 使用 `RefreshDatabase` trait，必要时使用 `DatabaseTransactions` 提升测试速度。
- 每个测试函数只用 `it()` 描述一个功能点。
- 测试描述应清晰说明"做了什么，期望什么结果"。
- 对 API 端点测试使用 `getJson()`、`postJson()` 等方法，并断言 HTTP 状态码和 JSON 结构。

## 示例结构
为 `app/Services/UserService.php` 生成测试时，应覆盖：
- **[正向]** 传入合法数据，验证用户创建成功，断言数据库存在对应记录。
- **[反向]** 传入重复邮箱，验证抛出验证异常，断言 422 状态码。
- **[边界]** 传入空值或超长字符串，验证 Form Request 校验是否生效。
```

#### Skill 三：Laravel 架构设计顾问 (`laravel-architect`)

参考 `laravel-architecture` Skill 关于 Services、Repositories、Actions 和 DDD 的实践，以及 `mguinea/laravel-ddd-example` 的六边形架构理念。

在 `.agents/skills/laravel-architect/` 目录下创建 `SKILL.md`：

```markdown
---
name: laravel-architect
description: >-
  为 Laravel 项目提供架构设计建议。
  当用户提到"设计模式"、"重构架构"、"分层"、"DDD"、"Repository"等关键词时使用。
  帮助设计 Services、Repositories、Actions、DTOs 和 Enums 的分层结构。
---

# Laravel 架构设计顾问

你是一位经验丰富的 Laravel 架构师，擅长领域驱动设计（DDD）、六边形架构和 SOLID 原则在 Laravel 中的实践。

## 核心原则
- **瘦控制器，富服务层**：控制器仅负责接收请求和返回响应，所有业务逻辑交由 Service 层处理。
- **接口隔离**：所有需要 Mock 的依赖（如第三方 SDK、支付网关）都应在 `app/Contracts/` 下定义接口。
- **单一职责**：每个类只做一件事。文件不应超过 200 行。
- **依赖注入优于 Facade**：在构造函数中注入依赖，使代码更易于测试。

## 推荐目录结构
```
app/
├── Actions/          # 单用途操作类（如 CreateUser、SendInvoice）
├── Contracts/        # 接口定义
├── DTOs/             # 数据传输对象（配合 spatie/laravel-data）
├── Enums/            # PHP 8.1+ 枚举
├── Events/           # 领域事件
├── Http/
│   ├── Controllers/  # 瘦控制器（每个方法不超过 7 行）
│   ├── Middleware/   # 请求过滤
│   └── Requests/     # Form Request 验证
├── Jobs/             # 队列任务
├── Models/           # Eloquent 模型（仅含关联、转换和简单访问器）
├── Policies/         # 授权策略
├── Repositories/     # 数据访问层
└── Services/         # 业务逻辑层
```

## 工作流程
1. **诊断现状**：分析用户项目当前的目录结构和代码组织，找出问题（如"胖控制器"、模型过重、耦合过紧）。
2. **提出方案**：给出具体的重构建议，并说明每一步带来的好处（可测试性、可维护性、可扩展性）。
3. **输出蓝图**：若用户要求，生成目标目录结构、关键类的代码骨架，以及迁移步骤。采用渐进式重构策略，避免大规模重写带来的风险。
```

---

### 💎 开源项目 "抄作业" 指北

除了以上为你生成的配置，以下是强烈推荐的"营养源泉"：

| 项目 | 地址 | 核心价值 |
|------|------|----------|
| **Spatie/guidelines-skills** | [github.com/spatie/guidelines-skills](https://github.com/spatie/guidelines-skills) | Spatie 官方开源 AI 编码技能，覆盖 Laravel/PHP、JS、版本控制、安全四大领域，直接通过 `skills.sh` 安装使用 |
| **Laravel Daily Cursor Rules** | [laraveldaily.com](https://laraveldaily.com/post/my-cursor-rules-for-laravel) | 实战派 AI 规则集合，作者持续更新（已更新 5 次以上），包含控制器注入策略、枚举使用规范等接地气的经验 |
| **multicoin/laravel-code-standards** | [packagist.org](https://packagist.org/packages/multicoin/laravel-code-standards) | 严格的 OOP 规范 + PHPStan Level 8 + Pint + PHPCS 一体化方案，CI/CD 就绪，开箱即用 |
| **mguinea/laravel-ddd-example** | [github.com/mguinea/laravel-ddd-example](https://github.com/mguinea/laravel-ddd-example) | 在 Laravel 中实践六边形架构 + DDD + CQRS 的完整示例，适合进阶架构参考 |
| **awesome-cursorrules** | [github.com/PatrickJS/awesome-cursorrules](https://github.com/PatrickJS/awesome-cursorrules) | 聚合各类技术栈的 `.cursorrules` 文件，包含 PHP/Laravel 部分，直接可转为通义灵码的 Rules |
| **anilcancakir/laravel-ai-sdk-skills** | [packagist.org](https://packagist.org/packages/anilcancakir/laravel-ai-sdk-skills) | Laravel AI SDK 的 Skill 系统，定义了标准的 YAML frontmatter + Markdown 格式，是理解 Skill 机制的优秀参考 |

### 📝 快速使用指南

1.  **存放规则**：在项目根目录创建 `.lingma/rules/` 目录，将上述四个 `.md` 文件放入其中。
2.  **存放 Skills**：在项目根目录创建 `.agents/skills/` 目录，在其下分别创建 `laravel-reviewer`、`pest-test-generator` 和 `laravel-architect` 三个子目录，每个子目录中放入对应的 `SKILL.md` 文件。
3.  **调用规则**：规则在 Agent 模式中自动生效，AI 在执行任何任务时都会遵循你定义的标准。
4.  **触发 Skills**：在对话框中输入 `/` 可以快速搜索并调用 Skills；或在 Agent 模式下用自然语言描述任务（如"帮我做一次代码审查"），Agent 会自动匹配并激活对应的 Skill。
5.  **持续迭代**：规则和 Skill 不是一成不变的。随着项目演进，你可以随时修改这些 `.md` 文件，让 AI 越来越懂你的团队规范。

有了这份面向 Laravel 的规则和技能框架，再结合开源社区的持续灵感汲取，AI 将成为你 Laravel 开发中更可靠、更懂你风格的合作伙伴。
